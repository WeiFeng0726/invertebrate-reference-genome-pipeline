# 复制为 paths.env 后按本机修改；可选：在运行前执行  source config/paths.env
# 当前仓库内脚本多数仍使用脚本内硬编码路径，发布到其它机器时请全局替换或改为读取环境变量。

export PROJECT_ROOT="/data/fengwei"
export DOWNLOAD_DIR="${PROJECT_ROOT}/download"
export GENOMES_DIR="${DOWNLOAD_DIR}/genomes"
export BUSCO_LINEAGE="${PROJECT_ROOT}/busco_db/busco_downloads/lineages/metazoa_odb10"
export CONDA_ENV_BUSCO="busco6"
